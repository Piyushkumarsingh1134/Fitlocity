spring:
  application:
    name: auth-service

  datasource:
    url: jdbc:postgresql://postgres:5432/fitlocity_gym
    username: ajaydangoriyatharu
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 4001

jwt:
  secret: this_is_a_very_long_secret_key_used_for_jwt_signing_which_must_be_at_least_256_bits_long
  expiry: 3600000