server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/fitlocity_gym
    username: ajaydangoriyatharu
    password: password

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

  flyway:
    enabled: true
    locations: classpath:db/migration