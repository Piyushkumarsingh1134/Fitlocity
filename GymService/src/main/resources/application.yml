server:
  port: 8082

spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/fitlocity_gym
    username: ajaydangoriyatharu
    password: password

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    enabled: false

jwt:
  secret: ${JWT_SECRET:this_is_a_very_long_secret_key_used_for_jwt_signing_which_must_be_at_least_256_bits_long}
  expiration: ${JWT_EXPIRY:86400000}